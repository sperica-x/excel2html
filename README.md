# excel2html

Excel copied table content to clean HTML table code.

## Usage

1. Copy a table range in Excel.
2. Paste it into the page.
3. Convert it to HTML.
4. Copy the generated `<table>` code.
5. Paste that code into CKEditor HTML mode.

## Deploy

This project is a static site and can be deployed directly on Vercel.
